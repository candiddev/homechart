package types

// Weekday represents a weekday as a number.
type Weekday int

// Weekday represents a weekday as a number.
const (
	Monday Weekday = iota + 1
	Tuesday
	Wednesday
	Thursday
	Friday
	Saturday
	Sunday
)
